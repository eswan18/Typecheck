//Some C++ Comments
//Nothing too tricky
/* and on to C style comments */
/* which can
extend over
multiple lines */

main: function integer (argc: integer, argv: array [] string)  = {
	i: integer = argc;
	for(i=0;i<40;i++) {
		print i;
	}
	if(argv[0] == "cminor")
		print "that's what I thought";
	return 0;
}
